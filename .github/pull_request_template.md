## Ticket

Link to the `docs/codex-backlog/NNN-*.md` ticket this implements.

## What changed

<!-- Concrete summary — files touched, behavior changed. -->

## Test evidence

<!-- Paste `uv run pytest` output. -->

```
$ uv run pytest
```

## Deploy/config impact

- [ ] No changes to `Dockerfile`, `fly.toml`, or env vars — or, if there are,
      `DEPLOY.md` was updated in this PR.
- [ ] No secrets, `.env`, or populated database files included in the diff.

## Ticket status

- [ ] Updated the ticket file's `Status` field to `in_progress` (or `done` if
      this PR is expected to fully close it) in `docs/codex-backlog/`.

## Open questions / blocked items

<!-- Anything left ambiguous per AGENTS.md's "don't guess, flag it" rule. -->
